{
  "//": "Claude Desktop: merge into claude_desktop_config.json (Settings > Developer > Edit Config).",
  "//note": "RECOMMENDED (works anywhere, no clone / no venv): uses `uvx` to build & run Investo",
  "//note2": "straight from GitHub. Requires `uv` installed (https://docs.astral.sh/uv/).",
  "mcpServers": {
    "investo": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/YashvantHange/Investo", "investo-mcp"]
    }
  },
  "//alternative": "From-source (no uv): clone + `pip install -e .` in a .venv, then command `python`",
  "//alternative2": "with args [\"<ABSOLUTE>/scripts/mcp_launcher.py\"] (the launcher finds the venv)."
}
