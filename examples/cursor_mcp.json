{
  "//": "Cursor MCP config for Investo. RECOMMENDED (works anywhere, no clone / no venv):",
  "//note": "uses `uvx` to build & run Investo straight from GitHub. Requires `uv` installed",
  "//note2": "(https://docs.astral.sh/uv/ — the Python equivalent of npx). Put this at",
  "//note3": ".cursor/mcp.json (project) or ~/.cursor/mcp.json (global) — both work.",
  "mcpServers": {
    "investo": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/YashvantHange/Investo", "investo-mcp"]
    }
  },
  "//alternative": "From-source (no uv): clone the repo, `python -m venv .venv && pip install -e .`,",
  "//alternative2": "then use { \"command\": \"python\", \"args\": [\"<ABSOLUTE>/scripts/mcp_launcher.py\"] }."
}
